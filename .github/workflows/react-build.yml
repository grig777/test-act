name: First pipeline
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name:
      working-directory: ./app
      run: npm install
    - name: Test
      working-directory: ./app
      run: npm run test
  